import Foundation

class Chinese {
    static func num99toChinese(_ num: Int) -> (chinese: String, pinyin: String)? {
        guard num >= 0 && num <= 99 else { return nil }
        return nil
    }
}
